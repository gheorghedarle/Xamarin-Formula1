<?xml version="1.0" encoding="utf-8" ?>
<StackLayout
    x:Class="Formula1.Views.Views.TeamInformationLoadingView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:templates="clr-namespace:Formula1.Views.Templates"
    Spacing="0">
    <templates:InformationLoadingTemplate />
    <templates:InformationLoadingTemplate />
    <templates:InformationLoadingTemplate />
    <templates:InformationLoadingTemplate />
    <Grid ColumnDefinitions="*, *">
        <templates:InformationLoadingTemplate Grid.Column="0" />
        <templates:InformationLoadingTemplate Grid.Column="1" />
    </Grid>
    <Grid ColumnDefinitions="*, *, *">
        <templates:InformationLoadingTemplate Grid.Column="0" />
        <templates:InformationLoadingTemplate Grid.Column="1" />
        <templates:InformationLoadingTemplate Grid.Column="2" />
    </Grid>
    <Grid ColumnDefinitions="*, *">
        <templates:InformationLoadingTemplate Grid.Column="0" />
        <templates:InformationLoadingTemplate Grid.Column="1" />
    </Grid>
</StackLayout>